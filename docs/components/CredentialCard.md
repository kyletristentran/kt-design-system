# CredentialCard

The credential card lists one license or certification with its number in mono.

## Consumer provides
- `.kt-credential` with `.kt-credential__title`, `.kt-credential__number` and `.kt-credential__detail`.

## Rules
- Numbers and versions use mono in `accent-deep`.
- Only list credentials that are current and verifiable.
